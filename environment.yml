name: rf-threat-detection
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14
  - pip
  - pip:
    - fastapi>=0.115.0
    - uvicorn[standard]>=0.30.0
    - websockets>=13.0
    - pydantic>=2.8.0
    - numpy>=2.0.0
    - scipy>=1.14.0
    - pytest>=8.0.0
    - httpx>=0.27.0
    - pyyaml>=6.0
